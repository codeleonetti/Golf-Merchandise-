

const intialState = {
    merchandise:[]
}

export const merchandiseReducer = (state, action) => {
    switch (type) {
        case :
            
            break;
    
        default:
            break;
    }
}